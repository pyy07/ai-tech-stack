# Copy to .env and fill in (never commit .env)
# Create at: https://github.com/settings/tokens
# Classic PAT with public_repo (or Fine-grained: Public repos read) is enough.
GITHUB_TOKEN=ghp_replace_me
